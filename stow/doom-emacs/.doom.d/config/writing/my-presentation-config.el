;;; $DOOMDIR/my-presentation-config.el -*- lexical-binding: t; no-byte-compile: t; -*-

;; Assuming 'emacs-reveal' is installed via package.el or straight.el,
;; manual load-path addition is usually not needed.
;; (add-to-list 'load-path "/Users/oscarvarto/.emacs.d/elpa/emacs-reveal/")
(require 'emacs-reveal)

(provide 'my-presentation-config)
